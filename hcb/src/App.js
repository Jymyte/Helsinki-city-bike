import { Button, Typography } from '@mui/material';

function App() {
  return (
    <div>
      <head>
        <meta name="viewport" content="initial-scale=1, width=device-width" />
      </head>
      <Button variant="contained">Hello world!</Button>
      <Typography>Jiihaa</Typography>
    </div>
  );
}

export default App;
